# Feature Flags

Список активных feature flags. Префикс `new_` = новое поведение, прячется до релиза. После raise-to-default флаг попадает в sweep на удаление.

| flag | default | owner | sprint | status | notes |
|------|---------|-------|--------|--------|-------|
| _none yet_ | | | | | |

## Sweep policy
- Если флаг включён в prod и стабилен — кандидат на удаление
- Если флаг не включали >2 спринтов — спросить владельца
